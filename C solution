#include<stdio.h>
int main()
{
    int k;
    scanf("%d",&k);
    
while(k--)
  {
    int a[100],i,n;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
      }
      for(i=0;i<n-1;i++)
      {
        if(a[i+1] > a[i])
        {
            printf("%d ",a[i+1]);
          }
          else
          {
              printf("%d ",-1);
          }  }
   printf("%d",-1);
   }
    return 0;
}
